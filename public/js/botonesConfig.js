//Configuramos Variables para nuestros id
/*let clubCambio = $("a#clubActivar");
let clubDesactivar = $("a#clubDesactivar");
//configuracion de los botones (activar desactivar)
$(function() {
    if ($(clubCambio).length > 0) {
        $('a').click(clubCambio, function(e) {
            e.preventDefault();
            console.log("hola");
            clubCambio.removeClass('btn btn-info').addClass('btn btn-danger').removeAttr('id').attr('id', 'clubDesactivar').html('Desactivar');
        });
    } else if ($(clubDesactivar).length > 0 && $(clubCambio).length < 0) {
        $('a').click(clubDesactivar, function(e) {
            e.preventDefault();
            console.log("adios");
            clubDesactivar.removeClass('btn btn-danger').addClass('btn btn-info').removeAttr('id').attr('id', 'clubActivar').html('Activar');
        });
    }
}); */
